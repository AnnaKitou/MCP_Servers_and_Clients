Install this mcp server by adding the following json code to your json config file
'''json
{
  "mcpServers": {
    "server": {
      "command": "uvx",
      "args": [
        "--from",
        "gitHubRepo.git",
        "chess"
      ]
    }
  }
}
'''
